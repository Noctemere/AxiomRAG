# Evaluation Package

This package will contain versioned datasets, retrieval benchmarks, answer-quality evaluations, and CI regression thresholds. Evaluation fixtures should not require paid model access to run basic checks.
